---
name: technical-defaults
description: Default technical choices for generated pages (Tailwind CDN, placeholders, responsiveness, file structure).
---

# Technical Defaults

- Use Tailwind CSS via CDN (`<script src="https://cdn.tailwindcss.com"></script>`)
- Use placeholder images from `https://placehold.co/` when source images aren't provided
- Mobile-first responsive design
- Single `index.html` file unless the user requests otherwise
